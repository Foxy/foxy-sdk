import type * as Core from '../../core';
import type * as Integration from '../../integration';

export interface FxDefaultShippingAddress extends Core.Graph {
  curie: Integration.Rels.FxDefaultShippingAddress['curie'];
  props: Integration.Rels.FxDefaultShippingAddress['props'];
}
